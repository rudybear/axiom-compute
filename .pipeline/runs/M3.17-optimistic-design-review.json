{
  "verdict": "APPROVE",
  "agent": "optimistic_design_reviewer",
  "milestone_id": "M3.17-runtime-checks",
  "pass": "r2",
  "strengths": [
    "CRITICAL-1 fix verified byte-for-byte against emit.rs: `uses_coopmat` (emit.rs:133-134, `body_uses_coopmat(tb)`) is the EXACT SAME boolean that gates `Capability::VulkanMemoryModel` (emit.rs:152-153) and the `MemoryModel::Vulkan` vs `MemoryModel::GLSL450` branch (emit.rs:158-161). §2/§4/§11's claim that the atomic-scope selection 'reuses the SAME uses_coopmat predicate that already drives the MemoryModel branch (emit.rs:152-162)' is not just plausible, it is a correct citation of the live code. QueueFamily-scope-under-Vulkan-memory-model needing no VulkanMemoryModelDeviceScope capability (vs. Device-scope requiring it) is the standard VK_KHR_vulkan_memory_model distinction — the fix is the textbook-correct one for this hole.",
    "CRITICAL-2 node-name fix verified: `HirStmt::BufferWrite` is real (crates/axc-hir/src/expr.rs:291-297) with fields `param_position: u32, buffer_binding: u32, index: HirExpr, value: HirExpr` — the spec's `BufferWrite { buffer_binding == buf, index == gid(0) }` matches the actual field names exactly (not `IndexAssign`, r1's loose name, now corrected everywhere in r2).",
    "The 'reuses body.rs:836' claim is honest once §5 and §11 are read together: `stmt_reassigns_binding_unconditionally` (body.rs:836) operates on `HirStmt::Assign{binding}`+`BindingId`, not `HirStmt::BufferWrite`, so it cannot be called verbatim on the new node kind. §11's Files section correctly says 'mirror body.rs:836 ... no descent into If/ForRange/While' (a new analogous function following the same non-descend discipline), while §5 says 'reuses the exact discipline' (the algorithmic rule, not the literal function). Read together this is coherent and not a broken claim — see minor concern below on tightening the wording so the Coder doesn't try to literally call the existing function on a BufferWrite match.",
    "Descriptor-plumbing fix (§7) is internally consistent with the actual runtime: `binding_plan.buffers.is_empty()` (context.rs:1628) and `binding_plan.buffers.len()` (context.rs:1636, pool sizing) both key off the same list the spec says the flag binding is APPENDED to — the 'all four derivations agree' claim is verifiable against the cited line numbers.",
    "NonCoherent readback fix (§2/§7) matches real infrastructure verbatim: `AXC_FORCE_NONCOHERENT_STAGING` env var and `force_noncoherent_staging` field both exist at context.rs:120-121/167/236, and `Coherency::NonCoherent` is a live enum variant (buffers.rs, persistent_map.rs) already used for user-buffer readback. AT-2879 exercises a real, existing mechanism, not a hypothetical one.",
    "The single-trailing-OpReturn assumption cited for postcondition exit-insertion is grounded: AT-323 ('return inside a loop is not yet supported in M1.3 (AT-323)') is a real, current limitation (body.rs:26,89,95,568) — mid-loop early return is genuinely deferred, so 'postcondition guards only the trailing return' is an accurate statement of the current codegen shape, not a hand-wave.",
    "AT-2861..2879 is a clean, gap-free, sequential range (19 tests) with no renumbering artifacts; the r1→r2 delta (+2877 guarded-write reject, +2878 NaN fail-loud, +2879 coherency fail-closed) lands exactly on the three r1 CRITICALs/gaps and nothing else, so no stray or orphaned AT was introduced.",
    "Restated LOC ceiling (650 target / 950 cap, delta +50/+50 from r1's 600/900) is internally consistent — both bumped by the same amount, matching the stated rationale ('+~50 for per-memory-model scope selection + the conservative top-level-dominance gate'), and the defer-list (§9) still correctly identifies shedding buffer-postconditions as the fallback that also removes CRITICAL-2's entire surface (AT-2877) if the cap is threatened.",
    "schema v4's `#[serde(default, skip_serializing_if = \"Option::is_none\")]` fix (my r1 minor + the pessimistic WARNING) is applied verbatim in §7/§11, and is the same idiom this file already uses for versioning discipline (metadata.rs:100-135, v1→v2→v3 precedent)."
  ],
  "concerns": [
    "Minor (wording precision, non-blocking): §5's 'It reuses the exact discipline already proven in body.rs:836 (stmt_reassigns_binding_unconditionally: ...)' could be misread by the Coder as 'call this function on BufferWrite,' which won't compile (the existing match arm only handles `HirStmt::Assign{binding}`). §11 already says 'mirror' correctly — recommend the Coder write a new, analogously-named function (e.g. a sibling in body.rs or lower.rs) rather than attempting literal reuse; worth a one-line QA note rather than a spec rewrite.",
    "Minor: AT-2867's coopmat leg (`@precondition(gt(K,0))` on a coopmat matmul) is stated as GPU-gated on 'real NVIDIA + Lavapipe' per the §12 header, but coopmat dispatch itself is tensor-core/NVIDIA-only per M3.1 — worth confirming in the AT text (or QA checklist) that the coopmat leg of AT-2867 is NVIDIA-only and Lavapipe is not expected to exercise it, to avoid a false CI expectation."
  ],
  "validation_against_existing": [
    "uses_coopmat / MemoryModel::Vulkan / Capability::VulkanMemoryModel gating verified live at emit.rs:133-134,152-162 — the r2 fix's predicate-reuse claim is a correct citation, not an assertion.",
    "HirStmt::BufferWrite{param_position,buffer_binding,index,value,span} verified live at crates/axc-hir/src/expr.rs:291-297 — the r1 'IndexAssign' naming error is fully corrected in r2.",
    "context.rs:1628/1636 binding_plan.buffers usage verified live — the r2 descriptor-plumbing claim ('all four derivations agree because the flag binding is appended') is checkable against the actual pool-sizing and layout code, not just asserted.",
    "AXC_FORCE_NONCOHERENT_STAGING / force_noncoherent_staging / Coherency::NonCoherent verified live at context.rs:120-236 and buffers.rs/persistent_map.rs — AT-2879 rides an existing, proven mechanism.",
    "AT-323 mid-loop-return deferral verified live at body.rs:26,89-95,568 — grounds the 'single trailing OpReturn' assumption stated for postcondition exit insertion.",
    "Vulkan memory model scope/capability distinction (QueueFamily needs no extra capability; Device scope under the Vulkan memory model requires VulkanMemoryModelDeviceScope) matches the VK_KHR_vulkan_memory_model / SPIR-V spec's documented capability requirements for OpAtomic* scope operands."
  ],
  "external_references": [
    "https://registry.khronos.org/SPIR-V/specs/unified1/SPIRV.html (Atomic Instructions, memory scope / capability requirements)",
    "https://registry.khronos.org/vulkan/specs/1.3-extensions/html/vkspec.html#memory-model (VulkanMemoryModelDeviceScope requirement for Device-scope atomics under the Vulkan memory model)",
    "https://registry.khronos.org/vulkan/specs/1.3-extensions/html/vkspec.html#synchronization-host (host read-back requires vkInvalidateMappedMemoryRanges for non-coherent memory)"
  ],
  "test_adequacy": "SUFFICIENT",
  "test_suggestions": [
    "Confirm in QA/AT-2867 text (or a code comment) that the coopmat leg is NVIDIA-only (tensor-core dispatch), so CI on Lavapipe-only runners doesn't silently skip the one AT that actually proves CRITICAL-1's fix.",
    "When the Coder implements the §5 dominance check, name the new function distinctly from `stmt_reassigns_binding_unconditionally` (e.g. `stmt_writes_buffer_unconditionally`) to avoid an accidental attempt to reuse the Assign-typed function on a BufferWrite match."
  ],
  "summary": "r2 coherently resolves all r1 pessimistic findings and my r1 minors, and every load-bearing citation was independently re-verified against the live source, not re-taken on faith. CRITICAL-1's QueueFamily/Device scope split keys off the literal same `uses_coopmat` boolean that already drives the memory-model branch (emit.rs:133-162) — confirmed by reading the code. CRITICAL-2's dominance gate now correctly names `HirStmt::BufferWrite{buffer_binding, index}` (verified against expr.rs:291-297, replacing r1's incorrect 'IndexAssign'); the 'reuses body.rs:836' language is honest when read as pattern-reuse (§11 correctly says 'mirror'), not literal-function-reuse, though I flag a minor wording-precision note for the Coder. Descriptor-plumbing and NonCoherent-readback fixes both cite real, checkable line numbers (context.rs:1628/1636, AXC_FORCE_NONCOHERENT_STAGING at context.rs:120-236) that match the actual runtime. The +3 ATs (2877/2878/2879) land exactly on the three fixed gaps with no orphaned or renumbered test, and the AT-2861..2879 range is gap-free. The restated LOC ceiling (650/950) is a consistent +50/+50 delta from r1 with a correctly-scoped defer-list fallback. APPROVE."
}
