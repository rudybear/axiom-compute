{
  "verdict": "APPROVE",
  "agent": "pessimistic_design_reviewer",
  "milestone_id": "M3.4-llamacpp-ab",
  "revision": "r1",
  "re_review_of": ".pipeline/runs/M3.4-pessimistic-design-review.json",
  "critical_issues": [],
  "resolved_critical": [
    "CRITICAL-1 (measurement-boundary comparability) — RESOLVED. §2 MANDATES the Coder open vendor/llama.cpp/tests/test-backend-ops.cpp at the pinned SHA and RECORD VERBATIM into ab_results.json (llamacpp_timer_boundary: quoted source excerpt + plain-language answers to GPU-timestamp-vs-CPU-wall, per-op-vs-batched-amortized, min/mean/sustained, overhead-subtracted) BEFORE timing anything; coder_handoff STEP 0 makes this the first action. §4 makes the AXIOM side COMPARABLE: it reports on-device GpuTimestamp MIN/MEAN/MEDIAN AND a sustained/batched CPU-wall number whose loop structure MIRRORS what STEP 0 found test-backend-ops does. The HEADLINE ratio is computed LIKE-FOR-LIKE against the matched boundary and LABELED; if boundaries genuinely cannot be matched (e.g. llama.cpp subtracts an overhead AXIOM cannot replicate), BOTH framings are reported and the difference DISCLOSED — the spec states explicitly it NEVER emits a single ratio silently comparing AXIOM-best-case to llama.cpp-sustained, and that min-vs-mean / per-op-vs-amortized are disclosed not hidden. AT-1766 makes QA confirm the RECORDED boundary MATCHES the actual source at the SHA (not merely that a block is present), closing the 'Coder records a convenient fiction' path. Residual best-case-vs-sustained bias is eliminated by construction: the only unmatchable axis (overhead subtraction) is routed to dual-framing + published, QA-audited disclosure, so no flattering ratio can survive silently.",
    "CRITICAL-2 (n==1 GEMV existence) — RESOLVED. §3 and §5 carry the HARD RULE: the Coder MUST verify a type_a=q4_K MUL_MAT n==1 (GEMV) case exists at the pinned SHA and record it verbatim. If ABSENT → kill_criterion_status='INCOMPLETE' with recorded reason + non-zero exit, NEVER a FAIL ratio. It is explicitly FORBIDDEN to substitute a GEMM (n>1) case as the kill number; GEMM may appear ONLY as labeled CONTEXT. Reinforced in AT-1767, the dedicated edge case, coder_handoff[3]+[7], and open_questions[4] is now a binding hard rule. INCOMPLETE is correctly a first-class outcome, not a substituted GEMM-FAIL."
  ],
  "warnings": [],
  "resolved_warnings": [
    "WARNING-3 (FLOP-convention parity) — RESOLVED. §3 ASSERTS llama.cpp's Q4_K GFLOPS counts ONLY 2*M*N*K matmul MACs (dequant excluded), matching AXIOM's 2*1*K formula, and adds a CONSISTENCY CHECK: recompute GFLOPS = 2*m*n*k/(us*1e-9)/1e9 and reject the line (status=INCOMPLETE) if it disagrees with llama.cpp's self-reported GFLOPS beyond ~2%. AXIOM TFLOPS is labeled 'matmul-equivalent FLOPs (dequant excluded)'. AT-1766 sub-assertion (flop_consistency.ok=true) audits it.",
    "WARNING-4 (literal SHA pin) — RESOLVED. §2 mandates LLAMACPP_COMMIT default be a LITERAL 40-char SHA (placeholder <PINNED_SHA_40> filled at clone time); the chosen b#### tag is resolved to its SHA ONCE and frozen; both tag AND 40-char SHA recorded in ab_results.json (commit.tag, commit.sha). Floating tags forbidden. AT-1766 requires commit.sha length 40.",
    "WARNING-5 (device-match fail-closed) — RESOLVED. §2 fails CLOSED (exit non-zero, no ratio) on ANY byte-difference between the two sides' device strings OR resolved ICD paths, regardless of model name; the 'RTX PRO 6000' substring check is explicitly SECONDARY, not an escape hatch; the 'or record if it differs' bypass is removed. AT-1766 (device_match.byte_identical=true) + edge case + coder_handoff[7] enforce it."
  ],
  "principle_violations": [],
  "spirv_concerns": [],
  "vendor_concerns": [
    "ACCEPTED (kept honest): §5 + DESIGN §3.1.19 prose qualify that an NVIDIA-only FAIL is the documented baseline and does NOT fire the DESIGN §5 'within 15% on ANY vendor' kill-criterion; AMD/Intel halves are deferred-not-dropped (EB.1). No output reads as 'kill criterion FAILED' without the 'NVIDIA only; criterion is any-vendor; AMD/Intel pending' qualifier. ROADMAP/BENCHMARKS edits mirror this."
  ],
  "missing_tests": [],
  "residual_risks_assessed": [
    "Coder must faithfully record the boundary (vs record a self-serving summary): BOUNDED — AT-1766 requires QA to confirm the recorded boundary MATCHES test-backend-ops.cpp at the SHA, and the source excerpt is committed for independent audit. A fabricated boundary is detectable from the committed excerpt.",
    "INCOMPLETE may be the honest outcome (no n==1, or boundary unmatchable beyond dual-framing): BOUNDED and ACCEPTABLE — INCOMPLETE is fail-closed (non-zero exit, recorded reason) and is explicitly NEVER rendered as a FAIL ratio. The worst honest outcome is a recorded INCOMPLETE, never a misleading number.",
    "Overhead-subtraction is the one boundary AXIOM cannot replicate: BOUNDED — §4 + the 'Measurement-boundary mismatch' edge case route this case to dual-framing (AXIOM-GpuTimestamp-MIN-vs-llama AND AXIOM-sustained-vs-llama) + explicit disclosure in ab_results.json and prose. No silent single ratio survives this axis."
  ],
  "new_holes": [],
  "questions_for_architect": [],
  "summary": "RE-REVIEW r1: all five r0 findings are RESOLVED with no new hole. CRITICAL-1 is closed not merely by 'record the boundary' but by AT-1766 forcing QA to verify the recorded boundary MATCHES the source at the pinned SHA, plus a comparable AXIOM side (min/mean/median + sustained CPU-wall mirroring llama.cpp's loop) and an explicit prohibition on any silent best-case-vs-sustained ratio — the only unmatchable axis (overhead subtraction) is routed to disclosed dual-framing. CRITICAL-2 is a binding HARD RULE: no n==1 Q4_K case → INCOMPLETE (non-zero exit, recorded reason), GEMM forbidden as the kill number and allowed only as labeled CONTEXT. WARNING-3 adds a real recompute-from-us FLOP-consistency gate (>2% → INCOMPLETE) with both sides asserted matmul-MACs-only dequant-excluded. WARNING-4 pins a literal 40-char SHA (tag+SHA both recorded). WARNING-5 fails closed on any device/ICD byte-difference. Honesty is fully preserved: real 144-byte Q4_K_M weights (the doubly-broken zero/64-byte existing bench flagged so QA never baselines it), expected-FAIL framing, NVIDIA-only-FAIL ≠ project-kill qualifier, mechanical verdict, llama.cpp gitignored, results committed. The three architect-flagged residual risks are execution-faithfulness risks, all bounded by AT-1766 source-match audit and fail-closed-INCOMPLETE. I confirm NO path to a silently-misleading ratio remains: the only honest outcomes are a like-for-like labeled ratio (with the boundary published) or a recorded INCOMPLETE. APPROVE — unblocks coding."
}
