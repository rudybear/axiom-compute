{
  "verdict": "IMPLEMENTED",
  "agent": "coder",
  "milestone_id": "M4.1-phase2-q4km-zero-copy-headline",
  "branch": "coder/M41p2-20260608/M4.1-q4km-zerocopy",
  "summary": "Ran the M3.6 Q4_K_M dequant+coopmat matmul (examples/q4km_matmul_rb_coopmat_f32acc_cached.axc, the 42.86-TFLOPS leader) as the HEADLINE op through the Phase-1 CUDA<->Vulkan zero-copy PyTorch frontend. Promoted the f32-accumulator oracle to a pub lib module (single source of truth), added the optional PyO3 strategy-compile kwarg + scalar_layout() + the Rust oracle bridge, wrote the Python Q4KMMatmul op (q upload-once + x/C per-call zero-copy), and proved AT-2103/2103c/2104/2103d/2105 on the real NVIDIA RTX PRO 6000. All hard gates pass; honest latency framing (no beat-cuBLAS). CODE-REVIEW FIXES applied (pessimistic REQUEST_CHANGES): W1 (blocking) + W2/W3/W5 (non-blocking) — see code_review_fixes.",

  "code_review_fixes": {
    "W1_contiguity_guard_BLOCKING": {
      "problem": "matmul() gated the genuine zero-copy (no-copy) path SOLELY on x.data_ptr()==xv.data_ptr() + dtype/device/dim/shape[0]==K, but NOT is_contiguous() nor the FULL shape. A same-storage NON-CONTIGUOUS view (e.g. xv.t()) at a SQUARE shape shares the data_ptr and passes shape[0]==K, so the copy was SKIPPED and the kernel read the underlying CONTIGUOUS storage as x[k*N+col] -> SILENTLY WRONG (the silent-reinterpret class the project forbids; not caught by the contiguous-fixture tests).",
      "fix": "py/axiom_compute/q4km.py — restructured the branch to `if x_f16.data_ptr() == xv.data_ptr():` (no-copy path) and ADDED a guard: if NOT x_f16.is_contiguous() OR tuple(x_f16.shape) != (self._k, n) -> raise ZeroCopyUnavailable (clear error; do NOT silently fall back to a copy on the zero-copy-gated path — a non-contiguous shared view is a usage error). The data_ptr-MISMATCH convenience branch (else) is UNCHANGED: it still does xv.copy_(x_f16.contiguous()). The genuine zero-copy happy path (a contiguous shared [K,N] view) STILL passes.",
      "test": "py/tests/test_zero_copy_q4km.py::test_q4km_zero_copy_rejects_noncontiguous_shared_view (W1 regression guard). At the SQUARE shape 256x256x256, takes xv.t() (asserts SAME data_ptr, SAME (k,n) shape, NOT contiguous), asserts op.matmul(xv_t) RAISES ZeroCopyUnavailable, then asserts the contiguous xv STILL works. PASSES on the NVIDIA RTX PRO 6000."
    },
    "W2_bundled_axc_sync_guard": {
      "problem": "The M3.6 kernel is duplicated into py/axiom_compute/kernels/q4km_matmul_rb_coopmat_f32acc_cached.axc for wheel self-containment and _load_kernel_src prefers the bundled copy; a future examples/ edit could leave the frontend silently running a STALE kernel.",
      "fix": "Added a byte-identity sync test py/tests/test_zero_copy_q4km.py::test_bundled_kernel_byte_identical_to_examples (no GPU needed) asserting the bundled copy == examples/ byte-for-byte; CI fails if they drift. (Chose the sync-test option over reordering _load_kernel_src.) PASSES."
    },
    "W3_oracle_doc_comment": {
      "problem": "crates/axc-driver/src/q4km_oracle.rs:6 doc header still said dependents `#[path]`-include it, but they were changed to `use axc_driver::q4km_oracle`.",
      "fix": "Updated the doc header to say the tests/benches `use axc_driver::q4km_oracle;` (NOT a #[path]-include). Doc-comment-only; no behavior change."
    },
    "W5_design_ratio_reconciliation": {
      "problem": "DESIGN.md §3.1.26 planning prose carried the pre-measurement '~34% of cuBLAS' while the IMPLEMENTED paragraph reported the measured '~53% (vs a measured f32 GEMM)' — read as contradictory.",
      "fix": "DESIGN.md §3.1.26: the AT-2105 planning line now says the kernel is 'below cuBLAS' and adds a parenthetical clearly labeling the two numbers — 34% is vs a 125-TFLOPS DATASHEET ESTIMATE, the MEASURED headline ~53% is vs a same-machine f32 torch.matmul cuBLAS GEMM; explicitly noted they are NOT contradictory. Also reconciled the q4km.py module docstring HONEST FRAMING line ('~34%' -> 'below cuBLAS (MEASURED ~53% of a same-machine f32 torch.matmul cuBLAS GEMM)')."
    }
  },

  "oracle_promotion": {
    "action": "git mv crates/axc-driver/tests/common_q4km_f32ref.rs -> crates/axc-driver/src/q4km_oracle.rs; added `pub mod q4km_oracle;` to crates/axc-driver/src/lib.rs; promoted `half` from [dev-dependencies] to [dependencies].",
    "including_targets_updated": "The 9 #[path]-including tests/benches now `use axc_driver::q4km_oracle as common_q4km_f32ref;` instead of re-including the file via #[path] — NO duplicate compile, NO duplicate test names, and the oracle's 5 #[cfg(test)] unit tests now run under `cargo test -p axc-driver --lib` (q4km_oracle::tests::{f32accum_ref_smoke, at1780_rootcause_cancellation_inflates_raw_rel_not_combined, f32accum_differs_from_naive_f16accum_at_larger_k, combined_metric_local_not_masked_by_global_scale, abs_dot_scale_dominates_output_magnitude}).",
    "regression_check": "cargo test --workspace green: 915 passed / 0 failed / 72 ignored (the oracle move did not regress any of the M3.x Q4_K_M tests/benches that consume it)."
  },

  "pyo3_surface": {
    "compile_kernel": "Added optional `strategy: Option<HashMap<String,i64>>` kwarg (pyo3 signature (source, device_uuid, strategy=None)). When Some -> BTreeMap -> axc_driver::compile_source_with_assignments (the M2.3 path the resident bench uses). When None -> compile_source_with_meta (Phase-1 saxpy path, verbatim).",
    "scalar_layout": "CompiledKernel::scalar_layout() -> Vec<(name, offset_bytes, size_bytes)> from meta.binding_plan.scalars (ScalarTy::bit_width()/8). Python assembles M/N/K/n_blocks_per_row by OFFSET+NAME, never positionally.",
    "oracle_bridge": "q4km_f32_reference(q, x_f16_bits: Vec<u16>, m, n, n_bpr) -> (y_ref, abs_scale); q4km_combined_metric(gpu, ref, abs_scale) -> f64 (the FROZEN gate, computed in Rust); q4km_raw_rel_diff(gpu, ref) -> f64 (report-only). All three call the IDENTICAL axc_driver::q4km_oracle functions."
  },

  "python_op": {
    "file": "py/axiom_compute/q4km.py",
    "Q4KMMatmul": "compile-once (full 5-hole leader strategy); upload_weights(q_bytes,m,k) registers static q; the 3-buffer SharedSession is allocated on the first matmul (N-dependent x/C sizes) and reused; q is written ONCE into the shared view (device-side copy, the static-weights carve-out). x_view()/c_view() expose the [K,N] f16 / [M,N] f32 shared views; matmul(x_f16) is genuinely zero-copy when x IS the shared view, else a DISCLOSED device-to-device copy (a [N,K] activation is rejected, never silently transposed). Push constants via assemble_push_constants (offset+name). Reuses the Phase-1 SharedSession handshake/teardown unchanged.",
    "zero_copy_disclosure": "The docstring carries the reviewer's honest note: the gated zero-copy path requires the activation already in [K,N] f16 in the shared view; a natural [N,K] inference activation would need a per-call transpose/repack (NOT the zero-copy gated path).",
    "init_threaded": "py/axiom_compute/__init__.py: compile_kernel(source, device, strategy) thread-through (a strategy/compile failure surfaces as AxiomError, distinct from ZeroCopyUnavailable); Kernel.scalar_layout()/push_constant_bytes()/assemble_push_constants(); re-exports Q4KMMatmul/q4km_matmul/pack_q4km/q4km_f32_reference/q4km_combined_metric/q4km_raw_rel_diff/Q4KM_SUPERBLOCK_BYTES.",
    "kernel_bundling": "The M3.6 .axc is bundled into the wheel at py/axiom_compute/kernels/ so the installed package is self-contained; q4km.py loads from the bundled copy or the repo examples/ dir."
  },

  "tests": {
    "file": "py/tests/test_zero_copy_q4km.py",
    "AT-2103": "PASS at K=256/512/14336. Combined condition-aware metric (computed in Rust): K256=5.348e-7 (raw 2.546e-2), K512=6.711e-7 (raw 1.072e0), K14336=2.053e-6 (raw 2.098e0). All <= FROZEN 1e-3; raw logged report-only. GPU + oracle read the SAME RNG q-byte/x-bit fixture (seeds 0xC0FFEE^m / 0xBADF00D^n, byte-identical to the resident bench).",
    "AT-2103c": "PASS. staging_buffers_allocated()==0 and copy_buffer_records()==0 after upload_weights AND across >=2 matmul() calls (upload is a device-side torch copy, NOT a dispatch-path vkCmdCopyBuffer).",
    "AT-2104": "PASS. 100 matmul() calls bit-identical to a single recompute (monotone V1/V2 handshake holds for the multi-buffer coopmat op).",
    "AT-2103d": "PASS. strategy=None on the M3.6 source FAILS at compile (UnresolvedStrategyHole); f32 x and CPU x both raise ZeroCopyUnavailable (fail-closed, no silent reinterpret).",
    "AT-2105": "PASS (the only hard latency gate). m=4096,n=512,k=14336: (a) AXIOM zero-copy 1.533 ms / 39.23 TFLOPS < (b) AXIOM host-copy 2.917 ms (win 1.384 ms, no host transfer); (c) torch.matmul f32 cuBLAS 0.806 ms / 74.64 TFLOPS CONTEXT ONLY. AXIOM ~52.6% of this f32-cuBLAS GEMM — kernel SLOWER than native torch.matmul; NO beat-cuBLAS claim (cuBLAS reported, not gated).",
    "W1-guard": "PASS (NEW). test_q4km_zero_copy_rejects_noncontiguous_shared_view: a transposed (non-contiguous) same-storage view of the shared buffer at the square shape RAISES ZeroCopyUnavailable; the contiguous shared view still passes.",
    "W2-sync": "PASS (NEW, no GPU). test_bundled_kernel_byte_identical_to_examples: bundled kernels/*.axc is byte-identical to examples/*.axc (drift guard).",
    "count": "9 q4km tests collected: 8 GPU-gated PASS + 1 non-GPU sync guard PASS (was 7; +W1 GPU guard +W2 non-GPU sync). 9 passed in 250s on the NVIDIA RTX PRO 6000."
  },

  "hygiene": {
    "cargo_check_workspace": "clean (re-run after the W3 doc-comment edit)",
    "cargo_clippy_all_targets_D_warnings": "clean (re-run after fixes)",
    "cargo_test_workspace": "915 passed / 0 failed (unchanged by the fixes — W3 is doc-only, W1/W2/W5 are Python/docs)",
    "maturin_wheel": "rebuilt (maturin build -m py + axiom_compute-0.0.1-cp312-abi3 wheel) + force-reinstalled; the edited q4km.py with the W1 guard is confirmed present in the installed package",
    "python_gpu_tests": "9 q4km tests PASS (incl. the new W1 GPU guard + W2 non-GPU sync) — AT-2103(256/512/14336)/2103c/2104/2103d/2105 all still PASS; the genuine zero-copy happy path unaffected. Phase-1 saxpy GPU tests unaffected (no Phase-1 code touched)."
  },

  "deviations": [
    "AT-2105's cuBLAS context number is an f32 GEMM (torch.matmul on f32-dequantized weights), measured ~0.81 ms / 74.7 TFLOPS on this box; AXIOM is ~53% of THAT f32-cuBLAS GEMM. The design's ~34% framing referenced the 125-TFLOPS datasheet ESTIMATE; the measured cuBLAS GEMM here is slower than the datasheet peak, so the measured ratio is higher. The honest conclusion is UNCHANGED and stated plainly: the Vulkan kernel is SLOWER than native torch.matmul, no beat-cuBLAS claim. Only the percentage differs because it is a measured-vs-measured ratio rather than vs the datasheet estimate.",
    "The M3.6 .axc source is duplicated into py/axiom_compute/kernels/ for wheel self-containment (a static fixture copy, not a second source of truth like the oracle would be). q4km.py loads the bundled copy or the repo examples/ dir.",
    "pack_q4km (the lossy Q4_K quantizer) is shipped for real-weight ergonomics but is NOT on the AT-2103 critical path (which uses the raw RNG fixture bytes so q is byte-identical on GPU + oracle), exactly as the architect scoped."
  ],

  "remaining": "QA + dual code review. Phase-1 residuals NOT touched (inherited, must-not-regress): no timeout on CUDA data-path waits; NVIDIA-only EXCLUSIVE-mode sync — Phase 2 added NO new sync.",

  "self_check": {
    "frozen_1e3_computed_in_rust_never_loosened": true,
    "per_call_xc_path_staging_free_0_0": true,
    "weights_upload_once_not_per_call_host_copy": true,
    "handshake_reused_phase1_verbatim_no_weakening": true,
    "honest_latency_no_beat_cublas": true,
    "strategy_kwarg_load_bearing_strategy_none_fails": true,
    "oracle_single_source_of_truth_promoted_not_reimplemented": true,
    "real_gpu_no_mocking": true,
    "branch_off_main_not_main": true
  }
}
